@model IEnumerable<WebApplication5.EstudianteCurso>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Curso.CodigoCurso)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estudiante.Identificacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Curso.CodigoCurso)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estudiante.Identificacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EstudianteCursoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EstudianteCursoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EstudianteCursoId })
        </td>
    </tr>
}

</table>
